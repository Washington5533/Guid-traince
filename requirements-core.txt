# Training Guardian Agent — 核心依赖（轻量安装）
# 安装: pip install -r requirements-core.txt
#   或:  pip install guarftrain  (通过 pyproject.toml)
#
# 前提: 训练环境已有 torch（guardian 不主动安装 torch）。
# 如需可视化/推理: pip install guarftrain[viz]
# 如需 AI 决策层:  pip install guarftrain[agent]
# 如需 MCP 接入:   pip install guarftrain[mcp]
# 如需 Web 面板:   pip install guarftrain[dashboard]
# 全部安装:        pip install guarftrain[full]

# === 核心依赖（必需） ===
pyyaml>=6.0

# === 硬件监控（可选，无 GPU 时自动降级） ===
psutil>=5.9
GPUtil>=1.4

# === 告警推送（可选，仅终端输出无需） ===
requests>=2.28

# === 数据处理 ===
numpy>=1.24

# === 以下为可选依赖，按需安装 ===
# AI 决策层:     pip install guarftrain[agent]      (anthropic)
# MCP 接入:      pip install guarftrain[mcp]        (mcp>=1.0)
# Web 面板:      pip install guarftrain[dashboard]  (fastapi, uvicorn)
# 可视化/推理:   pip install guarftrain[viz]        (torch, torchvision)
# 全部安装:      pip install guarftrain[full]
